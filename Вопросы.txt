1. Что за единица измерения - `fr`?

fr - от англ. fraction (частица, дробь, фракция) - гибкая единица измерения, которая задает для области занимать определенную долю от общего доступного пространства. С помощью fr определяется пространство, не занятое чем-либо еще. fr разделяет это пространство пропорционально.

2. Как можно задать грид с 5 колонками шириной по 20%? Минимум 2 способа.

1. grid-template-columns: 20% 20% 20% 20% 20%
2. grid-template-columns: repeat(5,20%)
3. grid-template-columns: 1fr 1fr 1fr 1fr 1fr

3. Самостоятельно разберитесь, что такое `auto-fill` и `auto-fit` ?

auto-fit и auto-fill - свойства, используемые внутри функции repeat. Они заменяют собой числа в repeat и вычисляют максимально возможное число колонок в контейнере.
auto-fill стремится поместить в строку как можно больше ячеек. Это свойство предпочитает использовать минимальную ширину для ячейки, даже если есть свободное место.
auto-fit стремится занять всё доступное пространство в строке и подстраивает ширину ячеек под это пространство.
В обоих случаях происходит перенос ячеек на новую строчку, количество колонок подстраивается под размер сетки. Но при наличии свободного пространства auto-fill использует минимальную ширину колонки, а auto-fit подстраивает ширину под свободное место. 
Значение auto-fill сохраняет размер колонки, даже если в ней нет содержимого.
При использовании значения auto-fit, пустые колонки схлопываются.

4. Как сделать такую табличку? Параметры: первая колонка шириной 100 пикселей, вторая 30%. Первая строчка высотой 200 пикселей, вторая строчка 100 пикселей.

Файл "Question 4"

5. Как сделать такое выравнивание в грид-контейнере? 

justify-content: space-between

6. Что такое и как задается *grid area*?

grid area - это пространство внутри grid контейнера, в которое может быть помещен один или больше grid элементов. Этот элемент может состоять из одной или более grid ячеек.
Задается двумя способами:
1. С помощью свойства grid-area, которое является сокращенным свойством для grid-row-start, grid-column-start, grid-row-end и grid-column-end, определяя размер и расположение элемента сетки, например: grid-area: 2 / 1 / 4 / 3.
2. С помощью задания грид-области имени и определения ее местоположения как значения свойства grid-template-areas. В этом случае не нужно задавать что-то отдельно для грид-элементов, все задается для грид-контейнера. Весь макет описывается значением свойства grid-template-areas.

7. Приведите пример использования `grid-template-areas` (не копированием из этого урока 😉)

 Файл "Question 7"

8. Каким свойством можно задать такое поведение элементов?

grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));

9. Самостоятельно разберитесь, как работают именованные линии? Есть ли какие-то рекомендованные правила наименований? Если да, то какие?

Имена линиям задаются внутри квадратных скобок в списке треков ( н-р: grid-template-rows: [header-start] 20vh [header-end] ).   После присвоения имен линиям можно использовать как их имена, так и их номера при позиционировании элементов на сетке. Имена линий могут содержать любые слова за исключением ключевого слова span.
Рекомендуется при именовании линий использовать суффикс -start для начальных линий (строки или колонки) и -end для конечных. 
Линии могут иметь несколько имен (н-р, в случае, когда конечная линия одной части сетки и начальная линия другой части совпадают). Задать линии несколько имён можно, добавив их через пробел внутри квадратных скобок  ( grid-template-columns: [full-start sidebar-start] 1fr 2fr [sidebar-end main-start] 1fr 2fr 1fr 2fr [main-end full-end] ).

10. Как проще всего задать 12 одинаковых по ширине колонок?  

grid-template-columns: repeat(12,1fr)

